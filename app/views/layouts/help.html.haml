!!!
%html
  %head
    %title Help
    :css
      body {
        font: 14px/24px "Lucida Grande", helvetica, arial, sans-serif;
        margin: 4em auto;
        width: 800px;
      }

      h1 {
        text-align: center;
      }

      section {
        margin: 0 auto;
        width: 42em;
      }

      footer {
        border-top: 1px solid #eee;
        color: #999;
        font-size: 0.8em;
        margin: 2em 0;
        padding-top: 1em;
        text-align: center;
      }

      footer a {
        color: #777;
        padding: 0 5px;
      }

      img {
        max-width: 100%;
      }

      p.note {
        background-color: #ffc;
        color: #767667;
        font-size: 0.8em;
        font-style: italic;
        padding: 1em;
      }
  %body
    = yield
    = render :partial => 'shared/chart_beat'
    %footer
      %p
        - if action_name != "index"
          = link_to "Return to Help", "/help"
          |
        = link_to "Salesflip", root_url
