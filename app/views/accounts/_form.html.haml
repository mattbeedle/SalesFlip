= error_messages_for form.object

%h3.toggler.open= t(:account)
.toggle
  %fieldset
    .50.a= form.input :name
    .50.z= form.input :account_type, :collection => Account.account_types

%h3.toggler.open=t(:contact_info)
.toggle
  %fieldset
    .66.a= form.input :email
    .33.z= form.input :phone
    .33.a= form.input :billing_address, :as => :text, :input_html => { :rows => 3 }
    .33= form.input :shipping_address, :as => :text, :input_html => { :rows => 3 }
    .33.z= form.input :fax
    .clear
    .100= form.input :website

%h3.toggler.open= t(:assignee)
.toggle
  %fieldset
    .25.a= form.input :assignee_id, :collection => current_user.company.users.asc(:email).map {|u| [u.email, u.id] }

.clear

.act.pad
  = form.button :submit
  = t(:or)
  = link_to t(:cancel), :back
